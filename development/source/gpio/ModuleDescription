/**
 * @brief
 * Libgpio is used to provide API to operate gpio.
 * libgpio.so have created in the directory /usr/lib/
 *
 * Operation method:
 * Refers to packages/example/Sample/gpio
 *
 * Add this module in the project:
 *
 * device/\<board\>/\<board\>_base.mk:PRODUCT_MODULES += libgpio
 *
 * @author Ingenic
 *
 * @version 1.0.0
 */
Libgpio Description;
